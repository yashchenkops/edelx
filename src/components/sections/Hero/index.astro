---
import Section from '@/components/layout/Section/index.astro';
import SectionTitle from '@/components/modules/SectionTitle/index.astro';
import usdImg from '@/assets/img/usd.webp';
import yenImg from '@/assets/img/yen.webp';
import euroImg from '@/assets/img/euro.webp';

const pathRight =
  'M595.973 0C595.32 5.70682e-08 594.716 0.344247 594.382 0.905378L464.236 219.941C461.569 224.43 456.734 227.181 451.514 227.181H443.799C435.626 227.181 429 233.809 429 241.985V267.015C429 275.191 435.626 281.819 443.799 281.819H451.514C456.734 281.819 461.569 284.57 464.236 289.059L594.381 508.095C594.714 508.656 595.319 509 595.971 509H828.071C828.797 509 829.24 508.202 828.856 507.585L687.945 281.558C686.932 279.932 685.152 278.943 683.236 278.943H505.667C497.493 278.943 490.867 272.314 490.868 264.138L490.868 244.86C490.869 236.684 497.495 230.057 505.668 230.057H683.238C685.153 230.057 686.934 229.068 687.947 227.442L828.859 1.41459C829.242 0.79834 828.799 -6.34854e-08 828.073 0H595.973Z';

const pathLeft =
  'M233.027 0C233.68 5.70682e-08 234.284 0.344247 234.618 0.905378L364.764 219.941C367.431 224.43 372.266 227.181 377.486 227.181H385.201C393.374 227.181 400 233.809 400 241.985V267.015C400 275.191 393.374 281.819 385.201 281.819H377.486C372.266 281.819 367.431 284.57 364.764 289.059L234.619 508.095C234.286 508.656 233.681 509 233.029 509H0.928623C0.202569 509 -0.240035 508.202 0.144059 507.585L141.055 281.558C142.068 279.932 143.848 278.943 145.764 278.943H323.333C331.507 278.943 338.133 272.314 338.132 264.138L338.132 244.86C338.131 236.684 331.505 230.057 323.332 230.057H145.762C143.847 230.057 142.066 229.068 141.053 227.442L0.141257 1.41459C-0.242478 0.79834 0.200829 -6.34854e-08 0.926755 0H233.027Z';

const SX = 1 / 829;
const SY = 1 / 509;
---

<Section class="hero" slide="main">
  <div class="hero__inner">
    <div class="hero__title">
      <SectionTitle as="h1">NEW WAY FOR FINANCE</SectionTitle>
    </div>

    <div class="hero__illustration">
      <div class="hero__currency hero__currency--back">
        <div class="hero__currency-item hero__currency-item--usd">
          <div class="hero__currency-reveal">
            <img src={usdImg.src} alt="USD" />
          </div>
        </div>
        <div class="hero__currency-item hero__currency-item--yen">
          <div class="hero__currency-reveal">
            <img src={yenImg.src} alt="YEN" />
          </div>
        </div>
      </div>

      <div class="hero__main">
        <div class="hero__glass" aria-hidden="true"></div>

        <svg
          class="hero__shape"
          width="829"
          height="509"
          viewBox="0 0 829 509"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
          aria-hidden="true"
        >
          <defs>
            <clipPath id="heroShapeClip" clipPathUnits="objectBoundingBox">
              <path transform={`scale(${SX}, ${SY})`} d={pathLeft}></path>
              <path transform={`scale(${SX}, ${SY})`} d={pathRight}></path>
            </clipPath>

            <linearGradient id="hero-border" x1="414.5" y1="0" x2="414.5" y2="509" gradientUnits="userSpaceOnUse">
              <stop offset="0%" stop-color="#fff" stop-opacity="0.04" />
              <stop offset="30%" stop-color="#fff" stop-opacity="0.14" />
              <stop offset="65%" stop-color="#fff" stop-opacity="0.38" />
              <stop offset="100%" stop-color="#fff" stop-opacity="0.72" />
            </linearGradient>
            <linearGradient id="hero-edge-highlight" x1="0" y1="0" x2="1" y2="0">
              <stop offset="0%" stop-color="#fff" stop-opacity="0" />
              <stop offset="18%" stop-color="#fff" stop-opacity="0.42" />
              <stop offset="82%" stop-color="#fff" stop-opacity="0.42" />
              <stop offset="100%" stop-color="#fff" stop-opacity="0" />
            </linearGradient>
            <filter id="hero-border-glow" x="-8%" y="-8%" width="116%" height="116%" color-interpolation-filters="sRGB">
              <feGaussianBlur in="SourceGraphic" stdDeviation="0.6" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          <g class="hero__shape-border" filter="url(#hero-border-glow)">
            <path d={pathRight} fill="none" stroke="url(#hero-border)" stroke-width="0.5" vector-effect="non-scaling-stroke" />
            <path d={pathLeft} fill="none" stroke="url(#hero-border)" stroke-width="0.5" vector-effect="non-scaling-stroke" />
          </g>

          <g class="hero__shape-edges" stroke="url(#hero-edge-highlight)" stroke-width="0.9" stroke-linecap="round">
            <line x1="12" y1="0.45" x2="222" y2="0.45" />
            <line x1="607" y1="0.45" x2="817" y2="0.45" />
            <line x1="155" y1="230.057" x2="314" y2="230.057" />
            <line x1="515" y1="230.057" x2="674" y2="230.057" />
          </g>
        </svg>
      </div>

      <div class="hero__currency hero__currency--front">
        <div class="hero__currency-item hero__currency-item--euro">
          <div class="hero__currency-reveal">
            <img src={euroImg.src} alt="EURO" />
          </div>
        </div>
      </div>
    </div>
  </div>
</Section>

<script>
  import { gsap } from 'gsap'

  const slide = document.querySelector<HTMLElement>('[data-slide="main"]')
  const currencies = Array.from(
    document.querySelectorAll<HTMLElement>('.hero__currency-reveal'),
  )

  if (slide && currencies.length) {
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    let played = false
    let isActive = slide.classList.contains('swiper-slide-active')

    gsap.set(currencies, {
      opacity: 0,
      y: 28,
      scale: 0.94,
      filter: 'blur(10px)',
    })

    const playIn = () => {
      if (reduced) {
        gsap.to(currencies, {
          opacity: 1,
          y: 0,
          scale: 1,
          filter: 'blur(0px)',
          duration: 0.25,
          overwrite: true,
        })
        return
      }

      gsap.fromTo(
        currencies,
        {
          opacity: 0,
          y: 32,
          scale: 0.94,
          filter: 'blur(10px)',
        },
        {
          opacity: 1,
          y: 0,
          scale: 1,
          filter: 'blur(0px)',
          duration: 0.85,
          delay: 0.28,
          stagger: 0.14,
          ease: 'power3.out',
          overwrite: true,
        },
      )
      played = true
    }

    const playOut = () => {
      if (!played) return
      gsap.to(currencies, {
        opacity: 0,
        y: -14,
        scale: 0.97,
        filter: 'blur(6px)',
        duration: reduced ? 0.2 : 0.28,
        stagger: 0.05,
        ease: 'power2.in',
        overwrite: true,
      })
    }

    const sync = () => {
      const nextActive = slide.classList.contains('swiper-slide-active')
      if (nextActive === isActive) return
      isActive = nextActive
      if (nextActive) playIn()
      else playOut()
    }

    new MutationObserver(sync).observe(slide, {
      attributes: true,
      attributeFilter: ['class'],
    })

    if (isActive) playIn()
  }
</script>

<style lang="scss">
  .hero__inner {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;

    @media (max-width: 768px) {
      flex-direction: column;
      gap: clamp(rem(70px), 8vw, rem(80px));
      justify-content: center;
      align-items: flex-start;
    }
  }

  .hero__title {
    max-width: 666px;

    @media (max-width: 768px) {
      max-width: 100%;
    }
  }

  .hero__illustration {
    position: relative;
    width: min(100%, 829px);
    aspect-ratio: 829 / 509;

    @media (max-width: 768px) {
      width: min(100%, 560px);
      align-self: center;
    }
  }

  .hero__main {
    position: relative;
    z-index: 1;
  }

  .hero__glass {
    position: absolute;
    inset: 0;
    z-index: 0;
    clip-path: url(#heroShapeClip);
    background: rgb(255 255 255 / 10%);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    pointer-events: none;

    :global([data-theme='light']) & {
      background: rgb(0 0 0 / 10%);
    }
  }

  .hero__shape {
    position: relative;
    z-index: 1;
    width: 100%;
    height: auto;
    overflow: visible;
    pointer-events: none;
    transform: translateZ(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__currency-item img {
      animation: none !important;
    }
  }

  .hero__currency {
    position: absolute;
    inset: 0;
    pointer-events: none;

    &--back {
      z-index: 0;
    }

    &--front {
      z-index: 2;
      /* Keep euro above the glass layer without isolating the glass parent */
      transform: translateZ(0);
    }
  }

  @keyframes hero-float-a {
    0%   { transform: translate(0px, 0px) rotate(0deg); }
    25%  { transform: translate(6px, -10px) rotate(1.2deg); }
    50%  { transform: translate(-4px, -18px) rotate(-0.6deg); }
    75%  { transform: translate(-8px, -8px) rotate(0.8deg); }
    100% { transform: translate(0px, 0px) rotate(0deg); }
  }

  @keyframes hero-float-b {
    0%   { transform: translate(0px, 0px) rotate(0deg); }
    30%  { transform: translate(-7px, -12px) rotate(-1.5deg); }
    60%  { transform: translate(5px, -20px) rotate(1deg); }
    80%  { transform: translate(8px, -10px) rotate(-0.5deg); }
    100% { transform: translate(0px, 0px) rotate(0deg); }
  }

  @keyframes hero-float-c {
    0%   { transform: translate(0px, 0px) rotate(0deg); }
    20%  { transform: translate(8px, -8px) rotate(1.8deg); }
    55%  { transform: translate(-5px, -16px) rotate(-1.2deg); }
    75%  { transform: translate(-9px, -6px) rotate(0.7deg); }
    100% { transform: translate(0px, 0px) rotate(0deg); }
  }

  .hero__currency-item {
    position: absolute;

    &--usd {
      width: min(300px, 36%);
      top: 50%;
      left: 20%;
      transform: translateY(-50%);

      img {
        animation: hero-float-a 7s ease-in-out infinite;
      }
    }

    &--yen {
      width: min(165px, 24%);
      top: -50px;
      right: 24%;

      img {
        animation: hero-float-b 8.5s ease-in-out infinite;
        animation-delay: -2.8s;
      }
    }

    &--euro {
      width: min(220px, 24%);
      bottom: -20%;
      right: 15%;

      img {
        animation: hero-float-c 9.2s ease-in-out infinite;
        animation-delay: -5.1s;
      }
    }
  }

  .hero__currency-reveal {
    opacity: 0;

    img {
      display: block;
      width: 100%;
      height: auto;
      position: relative;
      z-index: 1;
      will-change: transform;
    }
  }
</style>
